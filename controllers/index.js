const auth = require('./auth');
const rekon = require('./rekon');
const pagu_minus = require('./pagu-minus');
const seeding = require('./seeding');

module.exports = {
  auth,
  rekon,
  pagu_minus,
  seeding
}